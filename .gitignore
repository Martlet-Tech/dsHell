# Rust / Tauri build output
/target
**/target/
# 一次性构建目录：build.ps1 编到这里，为的是不动可能被运行中的 DShell
# 锁住的 target\。和 target\ 一样是产物，不该进仓库。
**/target-build/

# Tauri generated schemas (regenerated by tauri-build on every compile)
/src-tauri/gen/schemas/

# Editor / OS noise
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# Local logs and scratch files
*.log
