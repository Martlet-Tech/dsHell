# 在办升级项

放**正在进行**的升级项。一个升级项一份文档。

- 文件名 `NN-slug.md`，`NN` 为立项序号。
- 三段式：① 方案（文字，不含代码）② 关键代码 ③ 结案总结。
- 收口：写完第三段后移入 `../closed/`，同时删掉 `roadmap.md` 里对应条目。
- 在办期间可随意改。

详见 `../AGENTS.md`。

历史遗留：`01-env-doctor.md`、`02-env-doctor-code.md` 是旧的分轮写法（方案一份、代码一份），内容保持原样不合并、
不改写；该升级项已收口，两份文档已按规范移入 `../closed/`。新约定自后续立项起适用。

`05-attempts-summary.md` 是**复盘台账**（不是升级项，是 04 的历次尝试汇总），
按"一主题一文件"与 04 配套，已随 04 一起移入 `../closed/`。
