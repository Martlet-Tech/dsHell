{
  "name": "dshell-directory-picker",
  "version": "0.1.0",
  "private": true,
  "description": "DShell's native directory chooser backend for the DeepSeek Harness web GUI: delegates the folder picker to the DShell host so the dialog is parented to the main window and the renderer is never suspended",
  "license": "MIT",
  "type": "module",
  "main": "lib/index.js",
  "exports": {
    ".": {
      "default": "./lib/index.js"
    },
    "./cordis.patch.yml": "./cordis.patch.yml",
    "./package.json": "./package.json"
  },
  "files": [
    "lib/index.js",
    "cordis.patch.yml"
  ],
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    }
  },
  "peerDependencies": {
    "@deepseek-ai/cordis": "^4.0.2"
  }
}
